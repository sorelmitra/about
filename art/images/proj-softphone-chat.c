#include <learn.h>
#include <develop.h>

int softphone_and_chatapp() {
	while (1) {
		learn_some("Objective C");
		learn_some("Mac OS Cocoa");
		develop_some("Softphone Features);
		if (switch_to_chatapp()) break;
	}
	while () {
        learn_some("Angular JS");
        develop_some("Chat App Features);
        if (switch_to_softphone()) break;
	}
	while (1) {
        develop_some("Softphone Features);
	}
}

